# Resources managed by the service
#
# This file lists each resource, and can directly be applied to a kubernetes cluster
# by `kubectl apply`
apiVersion: extensions/v1beta1
kind: ThirdPartyResource
metadata:
  name: queue.aws.k8s.collaborne.com
description: "A specification of a SQS queue"
versions:
- name: v1
---
apiVersion: extensions/v1beta1
kind: ThirdPartyResource
metadata:
  name: role.aws.k8s.collaborne.com
description: "A specification of an IAM role"
versions:
- name: v1
---
